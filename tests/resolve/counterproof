# The counter-proof register for the Resolve tests. One line per check:
# what made that one check go red, against a DaVinci Resolve really
# running. Five fields, tab-separated, as in tests/state/counterproof:
#
#   test   when   the check's wording   what was broken   the red line
#
# It is here and not in tests/state/counterproof because
# source_checks_proved_test.py reads the tests out of tests/ and not out
# of tests/resolve/ -- rows there would be reported as belonging to no
# test at all. If the register is ever widened to this folder, these rows
# move across unchanged.
#
# The program was never edited for any of this: a copy went into scratch,
# the one thing was broken there, and the test ran against the copy with
# VPM_SCRIPT. Where the break is in resolve_ground.py or in the test
# itself, that is what the fourth field says.
project_settings_arrive	2026-08-31	the project the run asked for is the one Resolve has open	open_or_create_project creates the project as name + "-elsewhere"	the project the run asked for is the one Resolve has open  FAIL open is 'vpm-test-settings-63646-364a-elsewhere', asked for 'vpm-test-settings-63646-364a'
project_settings_arrive	2026-08-31	a name nobody used before is reported as created	open_or_create_project returns "opened" for a project it just created	a name nobody used before is reported as created           FAIL reported 'opened', expected 'created'
project_settings_arrive	2026-08-31	the saved project stands in the project manager's list	resolve_ground.OwnProject.open does not call pm.SaveProject()	the saved project stands in the project manager's list     FAIL 'vpm-test-settings-67108-191b' among 19 projects
project_settings_arrive	2026-08-31	the frame rate the handover names is the project's	apply_project_settings sends "30" whatever the handover names	the frame rate the handover names is the project's         FAIL project carries 30.0, handover said 25.0
project_settings_arrive	2026-08-31	a measured 23.98 reaches the project as 23.976	apply_project_settings sends "30" whatever the handover names	a measured 23.98 reaches the project as 23.976             FAIL project carries 30.0, expected 23.976
project_settings_arrive	2026-08-31	and 29.97 stays 29.97 rather than becoming 30	apply_project_settings sends "30" whatever the handover names	and 29.97 stays 29.97 rather than becoming 30              FAIL project carries 30.0, handover said 29.97
project_settings_arrive	2026-08-31	the timeline width the handover names is the project's	apply_project_settings swaps width and height in its list of settings	the timeline width the handover names is the project's     FAIL project carries '720', handover said 1280
project_settings_arrive	2026-08-31	the timeline height the handover names is the project's	apply_project_settings swaps width and height in its list of settings	the timeline height the handover names is the project's    FAIL project carries '1280', handover said 720
project_settings_arrive	2026-08-31	a second size arrives too, the first is not left standing	apply_project_settings swaps width and height in its list of settings	a second size arrives too, the first is not left standing  FAIL project carries '1080'x'1920', handover said 1920x1080, before that 1280x720
project_settings_arrive	2026-08-31	drop frame is off where the handover has it off	apply_project_settings turns the drop frame condition round	drop frame is off where the handover has it off            FAIL project carries '1', handover said off
project_settings_arrive	2026-08-31	drop frame is on where the handover has it on	apply_project_settings turns the drop frame condition round	drop frame is on where the handover has it on              FAIL project carries '0', handover said on
project_settings_arrive	2026-08-31	the same name a second time is opened, not created	open_or_create_project: "keep" falls through to the create-alongside branch	the same name a second time is opened, not created         FAIL reported 'created', expected 'keep'
project_settings_arrive	2026-08-31	and it is the project the test made, not another	open_or_create_project: "keep" falls through to the create-alongside branch	and it is the project the test made, not another           FAIL opened 'vpm-test-settings-67263-f609 2', expected 'vpm-test-settings-67263-f609'
project_settings_arrive	2026-08-31	there is still only one project of that name	the check run against a project list with the name put into it twice	there is still only one project of that name               FAIL 2 projects called 'vpm-test-settings-67316-e683'
project_settings_arrive	2026-08-31	the project the test made is gone again	resolve_ground.OwnProject.close does not call pm.DeleteProject	the project the test made is gone again                    FAIL 'vpm-test-settings-67450-55e4' is still in the project list
project_settings_arrive	2026-08-31	the run reached the end without an exception	open_or_create_project creates the project as name + "-elsewhere"	the run reached the end without an exception               FAIL RuntimeError: Project 'vpm-test-settings-63646-364a' could not be created. Is the name already in another folder?
project_pool_takes_all	2026-08-31	a clip came back for every file handed over	import_media hands back only the first pair of its assignment	a clip came back for every file handed over                FAIL 1 clips for 3 files
project_pool_takes_all	2026-08-31	the media pool itself holds one clip per file	import_media gives Resolve one file more than it was handed (a .wav beside the cameras)	the media pool itself holds one clip per file              FAIL 4 clips in the pool after 20.0 s, 3 files went in
project_pool_takes_all	2026-08-31	every clip carries the path of the file it came from	import_media gives Resolve one file more than it was handed (a .wav beside the cameras)	every clip carries the path of the file it came from       FAIL pool reports ['Kandidat_08141858_C009.mov', 'Moderator_REC00009.wav', 'Moderatoren_08141855_C005.mov', 'Totale_08141855_C003.mov'], files handed over ['Kandidat_08141858_C009.mov', 'Moderatoren_08141855_C005.mov', 'Totale_08141855_C003.mov']
project_pool_takes_all	2026-08-31	no two files were matched to the same clip	import_media matches every file to the first clip in the pool	no two files were matched to the same clip                 FAIL 1 distinct clips for 3 files
project_pool_takes_all	2026-08-31	every file handed over got a clip of its own back	import_media hands back only the first pair of its assignment	every file handed over got a clip of its own back          FAIL matched ['Kandidat_08141858_C009.mov']
project_pool_takes_all	2026-08-31	both files of the same name came back	import_media hands back only the first pair of its assignment	both files of the same name came back                      FAIL 1 clips for 2 files of one name
project_pool_takes_all	2026-08-31	the two are two clips, not one taken twice	import_media looks a clip up by name only: the path key and the collision guard taken out	the two are two clips, not one taken twice                 FAIL 1 distinct clips for ['cam0/C0001.MP4.mov', 'cam1/C0001.MP4.mov']
project_pool_takes_all	2026-08-31	each of the two is matched to the clip carrying its own path	import_media looks a clip up by name only: the path key and the collision guard taken out	each of the two is matched to the clip carrying its own path FAIL matched [('cam0', '/var/folders/5q/s2t85n2x41s3kzvw6q05jg4r0000gn/T/vpm_pool_pvu5llvt/cam0/C0001.MP4.mov'), ('cam1', '/var/folders/5q/s2t85n2x41s3kzvw6q05jg4r0000gn/T/vpm_pool_pvu5llvt/cam0/C0001.MP4.mov')]
project_pool_takes_all	2026-08-31	a file that does not exist is refused, not passed over	import_media answers a missing file with an empty assignment instead of raising	a file that does not exist is refused, not passed over     FAIL import_media said ''
project_pool_takes_all	2026-08-31	and it is refused for not being there, not for being lost	the existence guard at the head of import_media taken out, so Resolve is asked and the failure comes later	and it is refused for not being there, not for being lost  FAIL said 'Not found again after import: was-never-written.mov'
project_pool_takes_all	2026-08-31	and the refusal names the file that is missing	import_media answers a missing file with an empty assignment instead of raising	and the refusal names the file that is missing             FAIL 'was-never-written.mov' is not in ''
project_pool_takes_all	2026-08-31	the project the test made is gone again	resolve_ground.OwnProject.close does not call pm.DeleteProject	the project the test made is gone again                    FAIL 'vpm-test-pool-68682-2cf6' is still in the project list
project_pool_takes_all	2026-08-31	the run reached the end without an exception	import_media hands back only the first pair of its assignment	the run reached the end without an exception               FAIL KeyError: '/var/folders/5q/s2t85n2x41s3kzvw6q05jg4r0000gn/T/vpm_pool_kpnivewz/cam1/C0001.MP4.mov'
project_clips_land_right	2026-08-31	a clip asked onto a track that was never made is not on it	the check run against V1, a track that is really there, instead of V7	a clip asked onto a track that was never made is not on it FAIL V7 of a 1-track timeline holds None
project_clips_land_right	2026-08-31	and no such track appeared because it was asked for	a video track added between counting the tracks and the ask	and no such track appeared because it was asked for        FAIL 2 video tracks, 1 before the ask
project_clips_land_right	2026-08-31	so what the interface answers is no evidence on its own	an empty list handed to AppendToTimeline, so Resolve answers False	so what the interface answers is no evidence on its own    FAIL Resolve answered False to an insert onto a track that is not there
project_clips_land_right	2026-08-31	one video track per camera, and no more	build_camera_timeline makes one video track more than there are cameras	one video track per camera, and no more                    FAIL 4 video tracks for 3 cameras
project_clips_land_right	2026-08-31	no video track is left empty	build_camera_timeline makes one video track more than there are cameras	no video track is left empty                               FAIL empty video tracks: [4] of 4
project_clips_land_right	2026-08-31	one audio track per camera is left when it is tidied	remove_empty_audio_tracks returns at once and removes nothing	one audio track per camera is left when it is tidied       FAIL 6 audio tracks for 3 cameras
project_clips_land_right	2026-08-31	no audio track is left empty	remove_empty_audio_tracks returns at once and removes nothing	no audio track is left empty                               FAIL empty audio tracks: [4, 5, 6] of 6
project_clips_land_right	2026-08-31	the audio tracks carry them too	trim_audio_tracks does not call SetTrackName on the audio tracks	the audio tracks carry them too                            FAIL ['Audio 1', 'Audio 2', 'Audio 3'], expected ['Wide', 'Guest', 'Hosts']
project_clips_land_right	2026-08-31	the video tracks carry the speakers' names	build_camera_timeline does not call SetTrackName on the video tracks	the video tracks carry the speakers' names                 FAIL ['Video 1', 'Video 2', 'Video 3'], expected ['Wide', 'Guest', 'Hosts']
project_clips_land_right	2026-08-31	the timeline begins where the earliest camera does	earliest_offset returns 0.0 instead of the smallest camera offset	the timeline begins where the earliest camera does         FAIL begins at frame 90250, the earliest camera at 90150
project_clips_land_right	2026-08-31	%s alone on V%d, and it is its own file	build_camera_timeline makes no video tracks for the cameras	Guest alone on V2, and it is its own file                  FAIL V2 holds [], expected one 'Moderatoren_08141855_C005.mov'
project_clips_land_right	2026-08-31	%s starts at the frame its offset says	build_camera_timeline turns the sign of the camera offset round in recordFrame	Wide starts at the frame its offset says                   FAIL V1 starts at 90350, -4.0 s from the In point is frame 90150
project_clips_land_right	2026-08-31	%s goes on whole, not as a piece	build_camera_timeline sends startFrame 0 and endFrame 25 with every camera	Wide goes on whole, not as a piece                         FAIL V1 is 25 frames long, the file is 3000
project_clips_land_right	2026-08-31	the cut stays on one video track	build_cut_timeline adds a video track before it inserts	the cut stays on one video track                           FAIL 2 video tracks for a cut of one
project_clips_land_right	2026-08-31	every shot of the cut list is on the timeline	build_cut_timeline stops collecting shots after the second	every shot of the cut list is on the timeline              FAIL 2 shots on V1, the cut list names 3
project_clips_land_right	2026-08-31	the timeline begins at the In point	build_cut_timeline does not call SetStartTimecode	the timeline begins at the In point                        FAIL begins at frame 90000, the In point is 90250
project_clips_land_right	2026-08-31	each shot starts at the frame the cut list names	build_cut_timeline drops the shot's own offset from recordFrame	each shot starts at the frame the cut list names           FAIL shots start at [90250], the cut list says [90250, 90350, 90450]
project_clips_land_right	2026-08-31	each shot is as long as the cut list says	build_cut_timeline takes one frame off end_frame	each shot is as long as the cut list says                  FAIL shots are [99, 99, 99] frames, the cut list says [100, 100, 100]
project_clips_land_right	2026-08-31	no gap is left between one shot and the next	build_cut_timeline takes one frame off end_frame	no gap is left between one shot and the next               FAIL shots run [(90250, 90349), (90350, 90449), (90450, 90549)]
project_clips_land_right	2026-08-31	each shot comes from the camera the cut list names	piece() takes the first camera in the pool whatever the cut list names	each shot comes from the camera the cut list names         FAIL shots come from ['Kandidat_08141858_C009.mov', 'Kandidat_08141858_C009.mov', 'Kandidat_08141858_C009.mov'], the cut list names ['Kandidat_08141858_C009.mov', 'Moderatoren_08141855_C005.mov', 'Totale_08141855_C003.mov']
project_clips_land_right	2026-08-31	the cut's video track says what is on it	build_cut_timeline does not call SetTrackName on V1	the cut's video track says what is on it                   FAIL V1 is called 'Video 1', expected 'Camera cut'
project_clips_land_right	2026-08-31	the project the test made is gone again	resolve_ground.OwnProject.close does not call pm.DeleteProject	the project the test made is gone again                    FAIL 'vpm-test-clips-69553-753c' is still in the project list
project_clips_land_right	2026-08-31	the run reached the end without an exception	import_media hands back an empty assignment	the run reached the end without an exception               FAIL KeyError: '/tmp/vpm-fixtures-501/interview/Kandidat_08141858_C009.mov'
#
project_run_puts_back	2026-09-01	the decoy is in the project list to begin with	project_run_puts_back_test.py -- the two lines `made_decoy = pm.CreateProject(decoy) is not None` and `pm.SaveProject()` replaced by `made_decoy = True`, so no decoy is made	the decoy is in the project list to begin with             FAIL 'vpm-test-decoy-KEEP-60859' among 16 projects
project_run_puts_back	2026-09-01	and the decoy does not have the shape the sweep deletes	project_run_puts_back_test.py -- the decoy named "vpm-test-decoy-%d-beef" instead of "vpm-test-decoy-KEEP-%d", which is the sweep's whole shape	and the decoy does not have the shape the sweep deletes    FAIL 'vpm-test-decoy-60877-beef' against ^vpm-test-[a-z0-9]+-[0-9]+-[0-9a-f]{4}$
project_run_puts_back	2026-09-01	a folder of the tests' shape is in the folder list to begin with	project_run_puts_back_test.py -- `made_folder = bool(pm.CreateFolder(folder))` to `made_folder = True`, so no folder is made	a folder of the tests' shape is in the folder list to begin with FAIL 'vpm-test-folder-60908-8deb' among 0 folders
project_run_puts_back	2026-09-01	the run got as far as making its project	project_run_puts_back_test.py -- in the child, `step("project " + ground.name)` to `step("projekt " ...)`, so the parent never sees the step it waits on	the run got as far as making its project                   FAIL it got to 'projekt vpm-test-left-60956-3299' after 31.8 s
project_run_puts_back	2026-09-01	the run ended by itself, nothing had to be killed	project_run_puts_back_test.py -- `import time` and `while True: time.sleep(0.5)` appended to the child, so it never ends	the run ended by itself, nothing had to be killed          FAIL it ended with None after 30.2 s
project_run_puts_back	2026-09-01	a run that never tidied up leaves its project behind	resolve_ground.py -- `self.pm.SaveProject()` taken out of OwnProject.open	a run that never tidied up leaves its project behind       FAIL 'vpm-test-left-61212-374b' among 17 projects
project_run_puts_back	2026-09-01	and it leaves that project open in front of somebody	project_run_puts_back_test.py -- the child loads its own `ground.before` again after `ground.open()`	and it leaves that project open in front of somebody       FAIL Resolve has 'Untitled Project 1' open, the run made 'vpm-test-left-61258-7419'
project_run_puts_back	2026-09-01	the sweep deletes what that run left behind	resolve_ground.py -- `pm.DeleteProject(name)` in swept() to `pass`	the sweep deletes what that run left behind                FAIL 'vpm-test-left-61329-6e93' in the list: True, of 18 projects
project_run_puts_back	2026-09-01	and it says by name which project it deleted	sweep.py -- the swept line prints the count without the names	and it says by name which project it deleted               FAIL 'vpm-test-left-61348-1d32' in what the sweep printed: False
project_run_puts_back	2026-09-01	the sweep deletes a folder of the tests' shape as well	resolve_ground.py -- `pm.DeleteFolder(name)` in swept() to `pass`	the sweep deletes a folder of the tests' shape as well     FAIL 'vpm-test-folder-61365-274e' in the folder list: True, of 1 folders
project_run_puts_back	2026-09-01	a project that only looks like the tests' own is left alone	resolve_ground.py -- swept() takes `if "vpm-test" in n` for the projects instead of `if TEST_PROJECT.match(n)`	a project that only looks like the tests' own is left alone FAIL 'vpm-test-decoy-KEEP-61385' in the list: False, of 16 projects
project_run_puts_back	2026-09-01	everything the test made is gone again	project_run_puts_back_test.py -- `pm.DeleteProject(decoy)` in the finally to `pass`	everything the test made is gone again                     FAIL 'vpm-test-decoy-KEEP-61818' is still in the project list
project_run_puts_back	2026-09-01	the run reached the end without an exception	project_run_puts_back_test.py -- `listed()` calls `GetProjectListInCurrentFolderX`, a method Resolve does not have	the run reached the end without an exception               FAIL TypeError: 'NoneType' object is not callable
project_run_puts_back	2026-09-01	what the run remembers is a project it can open again	sweep.py -- in the --which branch `print(here)` to `print(here + "-nope")`, so the run remembers a name that stands in no project list	what the run remembers is a project it can open again      FAIL --which said 'Untitled Project 1-nope', among 16 projects: False
project_run_puts_back	2026-09-01	a project that was never saved is in no project list	project_run_puts_back_test.py -- `pm.SaveProject()` added right under `made_decoy = pm.CreateProject(decoy) is not None`, so Resolve writes the decoy out and it stands in the list	a project that was never saved is in no project list       FAIL 'vpm-test-decoy-KEEP-47446' among 17 projects: True
project_run_puts_back	2026-09-01	a project in no project list is not reported as open	sweep.py -- in the --which branch the clause `or here not in (pm.GetProjectListInCurrentFolder() or [])` taken out, so what is open is reported whether it can be loaded again or not	a project in no project list is not reported as open       FAIL --which said 'vpm-test-decoy-KEEP-47494' (ended with 0) while Resolve had 'vpm-test-decoy-KEEP-47494' open
project_run_puts_back	2026-09-01	the sweep is content where there was nothing to put back	sweep.py -- in the "nothing was open at the start to put back" branch `return 0` to `return 3`	the sweep is content where there was nothing to put back   FAIL sweep.py --restore '' ended with 3
project_run_puts_back	2026-09-01	and it does not ask for a project to be opened by hand	sweep.py -- the whole `if not wanted:` branch taken out, so an empty answer falls through to the loud box	and it does not ask for a project to be opened by hand     FAIL 'WOULD NOT OPEN' in what the sweep printed: True, of 296 characters
project_run_puts_back	2026-09-01	the project that was open at the start is open again	sweep.py -- `wanted = argv[at + 1]` to `wanted = argv[at + 1] + "-nope"`	the project that was open at the start is open again       FAIL Resolve has 'vpm-test-left-47631-0621' open, it started with 'Untitled Project 1'
project_run_puts_back	2026-09-01	and the sweep says by name which project it opened	sweep.py -- `print("  open again: %r" % wanted)` to `print("  open again.")`	and the sweep says by name which project it opened         FAIL 'Untitled Project 1' in what the sweep printed: False
